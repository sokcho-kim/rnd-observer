# R&D Agent - Teams 챗봇 구현 계획서

> **버전**: v1.0
> **작성일**: 2026-01-05
> **기반 문서**: rnd_agent_workflow.md

---

## 1. 프로젝트 개요

### 목표
R&D 공모전 참여 과정을 자동화하는 Teams 챗봇 시스템 구축

### 주요 기능
1. **공고 수집**: IRIS, NTIS, TIPA, aifactory 등 자동 스크래핑
2. **공고 분석**: 체크리스트/타임라인 자동 생성
3. **주제 추천**: 팀 역량 기반 공모전-주제 매칭
4. **자료 조사**: 논문, 기사, 경쟁 환경 자동 조사
5. **제안서 생성**: hwp/hwpx 양식에 맞는 초안 생성

---

## 2. 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                        Microsoft Teams                           │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                    Teams 채널/챗                            │ │
│  └──────────────────────────┬──────────────────────────────────┘ │
└─────────────────────────────┼───────────────────────────────────┘
                              │ Webhook / Bot Framework
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Azure Bot Service                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │ Message      │  │ Dialog       │  │ Adaptive     │           │
│  │ Handler      │  │ Manager      │  │ Cards        │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     FastAPI Backend                              │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    LangGraph Agent                        │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐        │   │
│  │  │Scraper  │ │Analyzer │ │Recomm.  │ │Research │        │   │
│  │  │Agent    │ │Agent    │ │Agent    │ │Agent    │        │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘        │   │
│  │  ┌─────────┐                                              │   │
│  │  │Writer   │                                              │   │
│  │  │Agent    │                                              │   │
│  │  └─────────┘                                              │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────┬───────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│   PostgreSQL     │ │     Redis        │ │   Blob Storage   │
│   - 공고 DB      │ │   - 캐시         │ │   - HWP 파일     │
│   - 조사 결과    │ │   - 세션         │ │   - 첨부파일     │
│   - 팀 프로필    │ │   - 스케줄러     │ │                  │
└──────────────────┘ └──────────────────┘ └──────────────────┘
```

---

## 3. 기술 스택

| 영역 | 기술 | 선정 이유 |
|------|------|-----------|
| **Bot Framework** | Python botbuilder | Teams 공식 지원, Python 생태계 활용 |
| **Agent Framework** | LangGraph | 복잡한 워크플로우, 상태 관리 우수 |
| **Backend** | FastAPI | 비동기 처리, 성능 우수 |
| **Database** | PostgreSQL | 구조화된 공고 데이터, 확장성 |
| **Cache** | Redis | 세션 관리, 스케줄러 |
| **File Storage** | Azure Blob / 로컬 | HWP 파일, 첨부파일 저장 |
| **HWP 생성** | python-hwp + hwpx 직접 생성 | 양식 기반 문서 생성 |
| **LLM** | Claude API / Azure OpenAI | Agent 추론, 문서 분석 |

---

## 4. 모듈별 상세 설계

### 4.1 Phase 1: 공고 스크래핑 모듈

#### 대상 사이트

| 사이트 | 스크래핑 방식 | 난이도 | 우선순위 |
|--------|---------------|--------|----------|
| IRIS | Selenium + BeautifulSoup | 중 | 1 |
| NTIS | REST API (있으면) / Selenium | 중 | 1 |
| TIPA | Selenium | 중 | 2 |
| aifactory | REST API | 하 | 1 |
| K-Startup | Selenium | 중 | 3 |

#### 데이터 스키마

```python
class Announcement(BaseModel):
    id: str                      # UUID
    source: str                  # IRIS, NTIS, TIPA 등
    title: str                   # 공고 제목
    url: str                     # 원본 URL
    organization: str            # 주최 기관
    category: list[str]          # 분야 (AI, 바이오 등)
    deadline: datetime           # 마감일
    budget: Optional[str]        # 지원 규모
    summary: Optional[str]       # 요약
    raw_content: str             # 원본 내용
    attachments: list[str]       # 첨부파일 URL
    scraped_at: datetime
    status: str                  # new, analyzed, applied, expired
```

#### Teams 명령어

```
@RnDBot /공고수집
@RnDBot /공고목록 [필터: AI, 바이오, 마감임박]
@RnDBot /공고알림 [on/off]
```

---

### 4.2 Phase 2: 공고 분석 모듈

#### 분석 결과 스키마

```python
class AnnouncementAnalysis(BaseModel):
    announcement_id: str

    # 기본 정보
    organizer: OrganizerInfo      # 주최/주관/운영
    purpose: str                  # 공모 목적
    target_fields: list[str]      # 대상 분야

    # 일정
    timeline: Timeline
    #   - application_start: datetime
    #   - application_end: datetime
    #   - preliminary_result: Optional[datetime]
    #   - main_period: Optional[DateRange]
    #   - final_result: Optional[datetime]

    # 체크리스트
    eligibility: list[CheckItem]  # 자격 요건
    restrictions: list[CheckItem] # 제한 사항
    deliverables: list[Deliverable] # 제출물
    #   - name: str
    #   - format: str (hwp, pdf, ppt 등)
    #   - template_url: Optional[str]

    # 평가
    evaluation_criteria: list[EvalCriterion]
    #   - name: str
    #   - weight: int (배점)
    #   - description: str

    # 지원 사항
    support: SupportInfo
    #   - budget: str
    #   - gpu: Optional[str]
    #   - mentoring: bool
    #   - etc: list[str]
```

#### Teams 출력 예시 (Adaptive Card)

```
┌─────────────────────────────────────────────┐
│ 📋 AI Co-Scientist Challenge Korea Track2   │
├─────────────────────────────────────────────┤
│ 주최: 과학기술정보통신부                     │
│ 주관: 한국연구재단                           │
│ 마감: 2026-01-02 (D-0)                       │
├─────────────────────────────────────────────┤
│ ⏰ 일정                                      │
│ • 신청: 2025.12.10 ~ 2026.01.02             │
│ • 예선발표: 2026.01.19                       │
│ • 본개발: 2026.01.19 ~ 04.03                │
├─────────────────────────────────────────────┤
│ ✅ 자격 요건                                 │
│ ☐ 개인 또는 팀 참가 가능                    │
│ ☐ 5인 이하 팀 구성                          │
├─────────────────────────────────────────────┤
│ 📊 평가 기준                                 │
│ • 파급성: 30점                               │
│ • 독창성: 30점                               │
│ • 구체성: 20점                               │
│ • 검증방법: 20점                             │
├─────────────────────────────────────────────┤
│ [상세보기] [체크리스트 저장] [주제추천]      │
└─────────────────────────────────────────────┘
```

---

### 4.3 Phase 3: 주제 추천 모듈

#### 팀 프로필 스키마

```python
class TeamProfile(BaseModel):
    team_id: str
    members: list[MemberProfile]

    # 기술 스택
    tech_stack: list[str]         # Python, LangGraph, Neo4j 등
    domains: list[str]            # RWE, NLP, 의료AI 등

    # 프로젝트 히스토리
    past_projects: list[Project]
    current_projects: list[Project]

    # 보유 자원
    resources: ResourceInfo
    #   - data: list[str]         # 보유 데이터
    #   - infra: list[str]        # GPU, 서버 등
    #   - partnerships: list[str] # 협력 기관
```

#### 추천 로직 (v1)

```python
def recommend_topics(announcement: AnnouncementAnalysis,
                     team: TeamProfile) -> list[TopicRecommendation]:
    """
    1. 공고 키워드 추출 (LLM)
    2. 팀 프로필과 매칭 점수 계산
    3. 추천 주제 3개 생성
    """
    # LangGraph Agent가 처리
    pass
```

#### Teams 명령어

```
@RnDBot /주제추천 [공고ID]
@RnDBot /팀프로필 설정
@RnDBot /적합도분석 [공고ID]
```

---

### 4.4 Phase 4: 자료 조사 모듈

#### 조사 소스

| 유형 | 소스 | 구현 방식 |
|------|------|-----------|
| 논문 | Google Scholar, arXiv, PubMed | SerpAPI + PDF 분석 |
| 기사 | 데일리팜, 팜뉴스, 전자신문 | 스크래핑 |
| 경쟁환경 | 웹 검색 | Tavily/SerpAPI |
| 시장규모 | 보고서, 통계청 | 웹 검색 + PDF 분석 |

#### 조사 결과 스키마

```python
class ResearchResult(BaseModel):
    announcement_id: str
    topic: str

    # 논문 분석
    papers: list[PaperSummary]
    #   - title, authors, year, abstract_summary
    #   - key_findings: list[str]
    #   - relevance_score: float

    # 경쟁 환경
    competitors: list[CompetitorAnalysis]
    #   - name, description, strengths, weaknesses

    # 시장 정보
    market_info: MarketAnalysis
    #   - size, growth_rate, key_players

    # 핵심 인사이트
    insights: list[str]
    gap_analysis: str              # "이게 없다" 발견점
```

#### Teams 명령어

```
@RnDBot /조사시작 [공고ID] [주제]
@RnDBot /논문검색 [키워드]
@RnDBot /경쟁분석 [주제]
@RnDBot /조사현황
```

---

### 4.5 Phase 5: 제안서 생성 모듈

#### HWP/HWPX 생성 전략

```
1. 템플릿 기반 접근
   - 공고 첨부 양식(hwp) 다운로드
   - 양식 필드 매핑 테이블 생성
   - LLM이 각 필드 내용 생성
   - python-hwp로 필드 채우기

2. HWPX 직접 생성 (양식 없을 때)
   - HWPX는 OOXML 기반 (zip + xml)
   - 표준 제안서 템플릿 보유
   - XML 직접 수정하여 생성
```

#### 제안서 구조

```python
class Proposal(BaseModel):
    announcement_id: str
    version: str

    sections: list[ProposalSection]
    #   - title: str
    #   - content: str (markdown)
    #   - evaluation_criteria: str  # 해당 평가항목
    #   - target_score: int

    # 생성된 파일
    files: ProposalFiles
    #   - markdown: str             # 원본 마크다운
    #   - hwpx_path: Optional[str]  # 생성된 hwpx
    #   - pdf_path: Optional[str]   # 변환된 pdf
```

#### Teams 명령어

```
@RnDBot /제안서작성 [공고ID]
@RnDBot /제안서검토 [버전]
@RnDBot /hwp생성 [버전]
@RnDBot /제안서히스토리
```

---

## 5. Teams 봇 인터페이스

### 5.1 주요 대화 흐름

```
사용자: @RnDBot 안녕

봇: 안녕하세요! R&D 공모전 도우미입니다.

    📌 주요 명령어:
    • /공고수집 - 새 공고 확인
    • /공고분석 [URL] - 공고 분석
    • /주제추천 [공고ID] - 적합 주제 추천
    • /조사시작 [공고ID] [주제] - 자료 조사
    • /제안서작성 [공고ID] - 제안서 초안 생성

    무엇을 도와드릴까요?
```

### 5.2 Adaptive Card 활용

- 공고 목록: 카드 형태로 표시, 필터 버튼
- 분석 결과: 섹션별 접기/펼치기
- 체크리스트: 인터랙티브 체크박스
- 파일 다운로드: 버튼으로 hwp 다운로드

### 5.3 알림 기능

```python
class NotificationConfig(BaseModel):
    team_id: str

    # 알림 설정
    new_announcement: bool = True     # 새 공고
    deadline_reminder: bool = True    # 마감 임박 (D-7, D-3, D-1)
    keywords: list[str] = []          # 키워드 필터
    categories: list[str] = []        # 분야 필터
```

---

## 6. 구현 로드맵

### Sprint 1 (2주): 기반 구축
- [ ] 프로젝트 구조 설정 (FastAPI + botbuilder)
- [ ] PostgreSQL 스키마 설계 및 마이그레이션
- [ ] Teams Bot 기본 연동 (echo bot)
- [ ] LangGraph 기본 구조 설정

### Sprint 2 (2주): Phase 1 - 스크래핑
- [ ] IRIS 스크래퍼 구현
- [ ] NTIS 스크래퍼 구현
- [ ] aifactory 스크래퍼 구현
- [ ] 공고 목록 Teams 출력 (Adaptive Card)
- [ ] 스케줄러 설정 (매일 자동 수집)

### Sprint 3 (2주): Phase 2 - 분석
- [ ] LLM 기반 공고 분석 Agent
- [ ] 체크리스트 생성 로직
- [ ] 타임라인 파싱
- [ ] 분석 결과 Teams 출력

### Sprint 4 (2주): Phase 3 - 주제 추천
- [ ] 팀 프로필 관리 기능
- [ ] 키워드 매칭 알고리즘
- [ ] 추천 Agent 구현
- [ ] 적합도 점수 계산

### Sprint 5 (3주): Phase 4 - 자료 조사
- [ ] 논문 검색 Agent (Scholar, arXiv)
- [ ] 뉴스 스크래핑 Agent
- [ ] 경쟁 환경 분석 Agent
- [ ] 조사 결과 종합 리포트

### Sprint 6 (3주): Phase 5 - 제안서 생성
- [ ] Markdown 제안서 생성
- [ ] HWPX 파일 생성 모듈
- [ ] 평가기준 기반 섹션 작성
- [ ] 버전 관리 기능

### Sprint 7 (2주): 통합 및 배포
- [ ] 전체 워크플로우 통합 테스트
- [ ] Teams 앱 패키징
- [ ] 팀 배포 및 문서화
- [ ] 피드백 수집 및 개선

---

## 7. 파일 구조

```
rnd-agent/
├── bot/                          # Teams Bot
│   ├── __init__.py
│   ├── main.py                   # Bot entry point
│   ├── dialogs/                  # 대화 흐름
│   │   ├── announcement_dialog.py
│   │   ├── research_dialog.py
│   │   └── proposal_dialog.py
│   ├── cards/                    # Adaptive Cards
│   │   ├── announcement_card.py
│   │   ├── checklist_card.py
│   │   └── proposal_card.py
│   └── handlers/
│       └── message_handler.py
│
├── agents/                       # LangGraph Agents
│   ├── __init__.py
│   ├── graph.py                  # 메인 Agent Graph
│   ├── scraper_agent.py          # Phase 1
│   ├── analyzer_agent.py         # Phase 2
│   ├── recommender_agent.py      # Phase 3
│   ├── researcher_agent.py       # Phase 4
│   └── writer_agent.py           # Phase 5
│
├── scrapers/                     # 스크래핑 모듈
│   ├── __init__.py
│   ├── base.py
│   ├── iris.py
│   ├── ntis.py
│   ├── tipa.py
│   ├── aifactory.py
│   └── news/
│       ├── dailypharm.py
│       └── etnews.py
│
├── services/                     # 비즈니스 로직
│   ├── __init__.py
│   ├── announcement_service.py
│   ├── research_service.py
│   ├── proposal_service.py
│   └── notification_service.py
│
├── document/                     # 문서 생성
│   ├── __init__.py
│   ├── hwpx_generator.py
│   ├── markdown_converter.py
│   └── templates/
│       └── proposal_template.hwpx
│
├── db/                           # 데이터베이스
│   ├── __init__.py
│   ├── models.py
│   ├── schemas.py
│   └── crud.py
│
├── api/                          # FastAPI
│   ├── __init__.py
│   ├── main.py
│   └── routes/
│       ├── announcements.py
│       ├── research.py
│       └── proposals.py
│
├── config/
│   ├── settings.py
│   └── prompts/                  # LLM 프롬프트
│       ├── analyzer.md
│       ├── recommender.md
│       └── writer.md
│
├── tests/
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
└── README.md
```

---

## 8. 주요 리스크 및 대응

| 리스크 | 영향 | 대응 방안 |
|--------|------|-----------|
| 사이트 구조 변경 | 스크래핑 실패 | 모니터링 + 알림, 모듈화된 파서 |
| Teams 인증 복잡 | 배포 지연 | Azure AD 사전 설정, 테스트 테넌트 |
| HWP 양식 다양성 | 자동화 한계 | 주요 양식 템플릿화, 수동 보완 안내 |
| LLM 비용 | 운영비 증가 | 캐싱, 작은 모델 우선 사용 |
| 저작권 이슈 | 논문/기사 인용 | 출처 명시, 요약만 저장 |

---

## 9. 성공 지표

| 지표 | 목표 |
|------|------|
| 공고 수집 커버리지 | 주요 5개 사이트 90% 이상 |
| 분석 정확도 | 체크리스트 항목 85% 이상 추출 |
| 주제 추천 만족도 | 팀원 평가 4.0/5.0 이상 |
| 제안서 초안 활용률 | 70% 이상 내용 채택 |
| 응답 시간 | 분석 요청 30초 이내 |

---

## 10. 다음 단계

1. **환경 설정**
   - Azure Bot Service 등록
   - Teams 앱 매니페스트 작성
   - 개발 환경 구축

2. **MVP 개발**
   - Sprint 1-2 진행
   - 기본 스크래핑 + Teams 연동

3. **팀 피드백**
   - MVP 배포 후 피드백 수집
   - 우선순위 조정

---

*이 계획서는 rnd_agent_workflow.md 기반으로 작성되었으며, 구현 과정에서 업데이트됩니다.*
